# Kimerby 블로그 사용법 가이드

Astro 기반 개인 블로그(kimerby.github.io)의 전체 운영 가이드입니다.

---

## 목차

1. [빠른 시작](#1-빠른-시작)
2. [메인 페이지 수정](#2-메인-페이지-수정)
3. [포스트 작성](#3-포스트-작성)
4. [시리즈 관리](#4-시리즈-관리)
5. [포트폴리오 수정](#5-포트폴리오-수정)
6. [About 페이지 수정](#6-about-페이지-수정)
7. [이미지 관리](#7-이미지-관리)
8. [사이트 설정 변경](#8-사이트-설정-변경)
9. [검색](#9-검색)
10. [배포](#10-배포)
11. [디렉토리 구조](#11-디렉토리-구조)

---

## 1. 빠른 시작

```bash
# 로컬 개발 서버 실행 (http://localhost:4321)
node_modules/.bin/astro dev

# 프로덕션 빌드 (dist/ 생성 + 검색 인덱스 생성)
npm run build

# 빌드 결과 로컬 미리보기
npm run preview
```

> `npm run build`는 내부적으로 `astro check → astro build → pagefind 인덱싱` 순서로 실행됩니다.

---

## 2. 메인 페이지 수정

### 인삿말(Hero) 수정

**파일:** `src/pages/index.astro`

페이지 상단 `<section id="hero">` 안의 내용을 직접 편집합니다.

```html
<!-- 제목 -->
<h1 class="...">Mingalaba</h1>

<!-- 소개 문구 -->
<p>
  여기에 원하는 소개 문구를 작성합니다.
</p>
```

### 소셜 링크 수정

**파일:** `src/constants.ts`

`SOCIALS` 배열에서 노출할 소셜 링크를 편집합니다. `href`만 채워진 항목은 표시되고, 빈 문자열이면 숨겨집니다.

```typescript
export const SOCIALS = [
  {
    name: "GitHub",
    href: "https://github.com/내아이디",   // 실제 URL로 교체
    linkTitle: "GitHub",
    icon: IconGitHub,
  },
  {
    name: "Mail",
    href: "mailto:내이메일@gmail.com",
    linkTitle: "Mail",
    icon: IconMail,
  },
  // 사용하지 않을 항목은 href를 "" 로 비워두면 숨겨짐
];
```

### Featured 포스트 노출

메인 페이지 상단 **Featured** 섹션에 노출하려면 포스트 frontmatter에 추가합니다.

```yaml
featured: true
```

### 최근 포스트 표시 개수

**파일:** `src/config.ts`

```typescript
postPerIndex: 5,  // 메인 페이지 Recent Posts 표시 개수
```

---

## 3. 포스트 작성

### 파일 위치 및 이름

```
src/data/blog/YYYY-MM-DD-제목.md
```

예) `src/data/blog/2025-06-01-새-포스트.md`

### 빠른 시작: 템플릿 복사

```bash
cp src/data/blog/_template.md src/data/blog/2025-06-01-새-포스트.md
```

### Frontmatter 필드 전체 레퍼런스

```yaml
---
title: "포스트 제목"                              # 필수
pubDatetime: 2025-06-01T09:00:00+09:00            # 필수 (ISO 8601, 서울 시간)
description: "포스트 한 줄 요약"                   # 필수 (목록 페이지 + SEO)
tags:                                             # 선택 (없으면 "others" 자동 부여)
  - 태그1
  - 태그2
author: 김어비                                     # 선택 (기본값: 김어비)
ogImage: ../../assets/images/폴더/이미지.jpg       # 선택 (없으면 자동 생성)
series: "시리즈 이름"                              # 선택 (series/ 파일명과 일치해야 함)
featured: false                                   # 선택 (true = 홈 상단 노출)
draft: false                                      # 선택 (true = 비공개/미발행)
modDatetime: 2025-06-10T12:00:00+09:00            # 선택 (수정일, 있으면 표시됨)
---
```

| 필드 | 필수 | 기본값 | 설명 |
|---|---|---|---|
| `title` | ✅ | — | 포스트 제목 |
| `pubDatetime` | ✅ | — | 발행일시 (ISO 8601) |
| `description` | ✅ | — | 한 줄 요약 |
| `tags` | — | `["others"]` | 태그 목록 |
| `author` | — | 김어비 | 작성자 |
| `ogImage` | — | 자동 생성 | 소셜 공유 썸네일 |
| `series` | — | — | 시리즈 소속 |
| `featured` | — | `false` | 홈 상단 Featured 노출 |
| `draft` | — | `false` | `true`이면 빌드에서 제외 |
| `modDatetime` | — | — | 수정일 (있으면 포스트에 표시) |

### 본문 작성

일반 마크다운 문법을 사용합니다.

**이미지 삽입:**

```markdown
![이미지 설명](../../assets/images/폴더/파일.jpg)
*이미지 캡션 (이탤릭으로 캡션 처리)*
```

**코드 블록:**

````markdown
```javascript
// 코드 작성
console.log("Hello");
```
````

**코드 블록에 파일명 표시:**

````markdown
```typescript
// src/utils/example.ts
export function hello() { ... }
```
````

---

## 4. 시리즈 관리

### 시리즈 파일 위치

```
src/data/series/시리즈명.md
```

### 시리즈 파일 형식

```markdown
---
title: 시리즈 표시 이름
description: 시리즈 목록 페이지에 표시될 한 줄 설명
ogImage: ../../assets/images/폴더/대표이미지.jpg
---

시리즈 소개 본문 (시리즈 상세 페이지에 표시됩니다)
```

### 시리즈와 포스트 연결 규칙

포스트의 `series` 값이 **시리즈 파일명과 정확히 일치**해야 합니다.

| 시리즈 파일 | 포스트 frontmatter |
|---|---|
| `src/data/series/격리일기.md` | `series: "격리일기"` |
| `src/data/series/aws.md` | `series: "aws"` |

### 기존 시리즈

| 시리즈명 | 파일 | 설명 |
|---|---|---|
| 격리일기 | `src/data/series/격리일기.md` | 2021년 중국 21일 격리 기록 |
| AWS | `src/data/series/aws.md` | AWS 아키텍처 스터디 |

---

## 5. 포트폴리오 수정

**파일 위치:** `src/data/portfolio/슬러그.md`

각 포트폴리오 항목은 별도 `.md` 파일로 관리합니다. 파일명이 URL slug가 됩니다.

### Frontmatter 필드

```yaml
---
title: "프로젝트 이름"
year: "2024"
category: "creative"     # "creative" 또는 "it" (섹션 구분)
type: "단편영화"           # 선택 — 제목 아래 작은 글씨로 표시
description: "한 줄 요약"  # 카드 목록에 표시
tags: ["태그1", "태그2"]
ogImage: ../../assets/images/폴더/파일.jpg  # 선택 — 카드 썸네일 + 디테일 히어로
links:                    # 선택 — 디테일 페이지 버튼
  - label: "버튼 레이블"
    href: "/링크"
order: 1                  # 카테고리 내 정렬 순서
cardHref: "/series/시리즈명"  # 선택 — 설정 시 카드 클릭이 디테일 페이지 대신 이 URL로 이동
---

디테일 페이지 본문 (마크다운)
```

### cardHref — 시리즈/외부 링크로 연결

디테일 페이지 없이 기존 시리즈나 외부 URL로 바로 연결하고 싶을 때 사용합니다.

```yaml
# 예: 격리일기는 시리즈 페이지로 바로 연결
cardHref: "/series/격리일기"
```

`cardHref`가 없으면 자동으로 `/portfolio/슬러그` 디테일 페이지로 이동합니다.

---

## 6. About 페이지 수정

**파일:** `src/pages/about.md`

frontmatter는 그대로 두고 그 아래 마크다운으로 자유롭게 작성합니다.

```markdown
---
layout: ../layouts/AboutLayout.astro
title: "About"
---

여기에 자유롭게 내용을 작성하세요.
```

---

## 7. 이미지 관리

### 이미지 저장 위치

```
src/assets/images/
├── profile/           # 프로필 사진
├── quarantine_diary/  # 격리일기 시리즈 이미지
├── portfolio/         # 포트폴리오 프로젝트 이미지
└── aws/               # AWS 스터디 이미지
```

새 폴더가 필요하면 위 경로 아래에 추가합니다.

### Astro 이미지 최적화

`src/assets/images/` 안에 넣은 이미지는 Astro가 자동으로:
- WebP 포맷으로 변환
- 화면 크기에 맞게 리사이징
- lazy loading 적용

### 본문에서 이미지 경로

포스트(`src/data/blog/`) 기준으로 `../../`로 거슬러 올라가야 합니다.

```markdown
![설명](../../assets/images/폴더/파일.jpg)
```

---

## 8. 사이트 설정 변경

**파일:** `src/config.ts`

```typescript
export const SITE = {
  website: "https://kimerby.github.io/",
  author: "김어비",
  title: "Kimerby | 김어비",          // 브라우저 탭 타이틀
  desc: "Product Manager & Creator",  // 사이트 설명 (SEO)
  logo: "/images/profile/film.jpeg",  // 헤더 로고 이미지
  postPerIndex: 5,                    // 홈페이지에 표시할 최근 포스트 수
  postPerPage: 5,                     // 목록 페이지당 포스트 수
  showArchives: true,                 // 아카이브 메뉴 표시 여부
  showBackButton: true,               // 포스트 내 뒤로가기 버튼 표시 여부
  dynamicOgImage: true,               // 포스트별 OG 이미지 자동 생성
  lang: "ko",
  timezone: "Asia/Seoul",
};
```

---

## 9. 검색

블로그 내 전문 검색은 **Pagefind** 기반으로 동작합니다.

- `npm run build` 시 자동으로 검색 인덱스 생성
- 검색 페이지: `https://kimerby.github.io/search` (또는 로컬 `http://localhost:4321/search`)
- 로컬 개발 서버(`astro dev`)에서는 검색이 동작하지 않음 → 확인하려면 `npm run build && npm run preview` 사용

---

## 10. 배포

로컬에서 작업 후 `master` 브랜치에 push하면 GitHub Actions가 자동으로 빌드하고 배포합니다.

```bash
git add .
git commit -m "포스트 추가: 새 글 제목"
git push origin master
```

- 배포 URL: https://kimerby.github.io
- 배포 소요 시간: 보통 1~2분
- 배포 상태 확인: GitHub 저장소 → Actions 탭

---

## 11. 디렉토리 구조

```
kimerby.github.io/
├── src/
│   ├── assets/
│   │   └── images/          # 블로그 이미지 (WebP 자동 최적화)
│   ├── components/          # 재사용 UI 컴포넌트 (헤더, 카드 등)
│   ├── data/
│   │   ├── blog/            # 블로그 포스트 .md 파일
│   │   └── series/          # 시리즈 정의 .md 파일
│   ├── layouts/             # 페이지 레이아웃 템플릿
│   ├── pages/
│   │   ├── index.astro      # 홈페이지
│   │   ├── about.md         # About 페이지
│   │   ├── portfolio.astro  # 포트폴리오 페이지
│   │   ├── search.astro     # 검색 페이지
│   │   ├── posts/           # 포스트 목록 + 상세 라우트
│   │   ├── series/          # 시리즈 목록 + 상세 라우트
│   │   └── tags/            # 태그별 필터 라우트
│   ├── utils/               # 유틸리티 함수 모음
│   └── config.ts            # 사이트 전역 설정
├── public/                  # 정적 파일 (그대로 복사됨)
├── CLAUDE.md                # Claude AI 편집 가이드
├── 사용법.md                 # 이 파일
└── astro.config.ts          # Astro 빌드 설정
```
